HOST=127.0.0.1
PORT=3001
# Set NODE_ENV=production for public deployment; configure both secrets first.
# NODE_ENV=production
# ADMIN_PASSPHRASE=replace-with-a-unique-secret-of-at-least-12-characters
# MAGIC_SCHOOL_SECRET=replace-with-at-least-32-random-characters
# SERVE_DIST=1
